# Production (mode=production). Prefer setting in CI; override with .env.local if needed.
# Required: API base URL (all API calls and image origin). No default in code.
VITE_APP_BASE_URL=http://129.212.226.33/api
# Optional: CSP img-src origins (uploads from same host).
# VITE_CSP_IMG_SRC=http://129.212.226.33

VITE_APP_API_KEY=
VITE_GOOGLE_CLIENT_ID=
VITE_APPLE_CLIENT_ID=
VITE_USE_COOKIE_AUTH=false
